import { Card, CardTitle } from "@/src/components/ui/card";

export default function page() {
    return(
        <Card>
            <CardTitle className="font-script text-4xl">Évènements</CardTitle>
        </Card>
    );
}